{
  "name": "telegram-admin-server",
  "version": "1.0.0",
  "description": "Admin server for Telegram bots with users, payments, broadcasts, promo codes",
  "main": "lib/index.js",
  "files": ["lib", "README.md"],
  "scripts": {
    "build": "babel src --out-dir lib --copy-files",
    "dev": "npm run build -- --watch",
    "link": "npm link",
    "pack": "npm run build && npm pack",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["telegram", "bot", "admin", "panel", "api"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "joi": "^17.13.3"
  },
  "peerDependencies": {
    "mongoose": "^7.0.0 || ^8.0.0",
    "telegraf": "^4.16.0"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@babel/cli": "^7.24.0",
    "@babel/preset-env": "^7.24.0"
  }
}
